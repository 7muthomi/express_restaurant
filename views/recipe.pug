extends layout 
block content
    h1(class="font-bold text-green-400") Random Recipes
     ul
      each recipe in recipes
        li
          h2= recipe.title
          img(src=recipe.image, alt=recipe.title, width="200")
          p= recipe.summary
          a(href=recipe.sourceUrl target="_blank") View Recipe